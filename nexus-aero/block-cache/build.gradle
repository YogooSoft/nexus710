/*
 * Nexus 710 Aero Storage — Block Cache
 * Multi-level block cache (heap + mmap SSD + S3) with weighted LRU eviction
 */

apply plugin: 'java-library'

repositories {
    mavenCentral()
}

group = 'io.nexus710'

dependencies {
    api project(':server')
}
