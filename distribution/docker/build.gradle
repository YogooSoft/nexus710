/*
 * Nexus 710 Docker build configuration.
 * Docker images are built using the root Dockerfile instead of the legacy ES docker build.
 * This build file is a simplified placeholder.
 */

apply plugin: 'base'
