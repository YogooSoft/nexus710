/*
 * Nexus 710 Precision Ranker — Feature Join
 * Real-time feature association during search fetch phase for precision re-ranking
 */

apply plugin: 'java-library'

repositories {
    mavenCentral()
}

group = 'io.nexus710'

dependencies {
    api project(':server')
}
